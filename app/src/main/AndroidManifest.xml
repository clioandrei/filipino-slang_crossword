<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.filipino_slang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_official_icon"
        android:label="Filipino Slang: Crossword Edition"
        android:roundIcon="@mipmap/app_official_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Filipinoslang">
        <activity android:name=".GameScreen"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>